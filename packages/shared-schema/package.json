{
  "name": "@game-forge/shared-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript interfaces, enums, and JSON schemas for Game Forge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.7.0"
  }
}
